// Canonical route definitions live in utils/apiRoute.js (API_Route).
// This file re-exports them under the legacy `endpoints` shape for compatibility.
export { API_Route as endpoints } from './utils/apiRoute';
